import React from 'react'
import HeroCarousel from './HeroCarousel'
import Nav from './Nav'

export default function Hero(){

    return (
        <div className="hero">
            <Nav/>
            <HeroCarousel/>
        </div>

    )

}